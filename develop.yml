# requirements for tabbed's development environment
name: tabbed
dependencies:
    - ipython
    # testing
    - pytest
    # Linting & format
    - isort
    - mypy
    - pylint
    # pypi packages
    - pip
    - pip:
        - clevercsv
        # formatting
        - black
        # spell checking
        - codespell
        # versioning
        - bumpver
        # doc builders
        - mkdocs
        - mkdocs-material
        - "mkdocstrings[python]"
        - mkdocs-execute-plugin
        # packaging
        - pip-tools
        - build
        - twine
